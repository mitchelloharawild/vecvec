% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/array.R
\name{vecvec_abind}
\alias{vecvec_abind}
\title{Combine vecvec arrays along an arbitrary dimension}
\usage{
vecvec_abind(..., along = 1L)
}
\arguments{
\item{...}{Vectors or dim-annotated \code{vecvec} arrays to combine; wrapped
with \code{\link[=vecvec]{vecvec()}} automatically if not already \code{vecvec} objects. Named
arguments (e.g. \code{vecvec_abind(a = x, b = y)}) name the corresponding
position(s) along \code{along}, similar to \code{\link[=cbind]{cbind()}}/\code{\link[=rbind]{rbind()}}.}

\item{along}{The dimension along which to combine \code{...}. Defaults to \code{1}
(row-wise, as \code{\link[=rbind]{rbind()}}). The result always has at least
\code{max(2, along)} dimensions, so binding plain vectors produces a matrix
(like \code{\link[=cbind]{cbind()}}/\code{\link[=rbind]{rbind()}}) rather than a flat vector - use \code{\link[=c]{c()}} for
flat concatenation.}
}
\value{
A \code{vecvec} array whose dimension at \code{along} is the sum of the
\code{along} dimensions of \code{...} (after padding), and whose other dimensions
match \code{...}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{vecvec_abind()} generalises \code{\link[=cbind]{cbind()}}/\code{\link[=rbind]{rbind()}} to arbitrary-dimensional
arrays, combining \code{...} along the dimension given by \code{along}. It underlies
the \code{cbind()}/\code{rbind()} methods for \code{vecvec} (\code{along = 2} and \code{along = 1}
respectively), and can also be used directly to stack higher-dimensional
\code{vecvec} arrays, e.g. combining two matrices into a 3D array with
\code{along = 3}.

Inputs that aren't already \code{vecvec} objects are first wrapped with
\code{\link[=vecvec]{vecvec()}}. An input with no \code{dim} (a plain vector) is treated as having a
single dimension of length \code{1} at \code{along}, so a bare vector becomes a
single column (\code{along = 2}), row (\code{along = 1}), or slice (\code{along > 2}) -
matching how \code{\link[=cbind]{cbind()}}/\code{\link[=rbind]{rbind()}} treat plain vectors. Every dimension
other than \code{along} must match (after this padding) across all inputs.
}
\examples{
# cbind()-like: bind columns
vecvec_abind(vecvec(1:3), vecvec(4:6), along = 2)

# rbind()-like: bind rows (the default)
vecvec_abind(vecvec(1:3), vecvec(4:6))

# Stack two matrices into a 3D array
m1 <- array(vecvec(1:6), dim = c(2, 3))
m2 <- array(vecvec(7:12), dim = c(2, 3))
vecvec_abind(m1, m2, along = 3)

}
\seealso{
\code{\link[=vecvec]{vecvec()}}; \code{\link[=c]{c()}} for flat (dim-less) concatenation of \code{vecvec}
objects.
}
