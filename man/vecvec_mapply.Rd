% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply.R
\name{vecvec_mapply}
\alias{vecvec_mapply}
\title{Apply a vectorised function across multiple vectors of a vecvec, batched
by shared underlying storage slots}
\usage{
vecvec_mapply(.l, .f, ..., ptype = NULL, SIMPLIFY = !is.null(ptype))
}
\arguments{
\item{.l}{A list of vectors to apply \code{.f} over. Elements of \code{.l} may be
\code{vecvec} objects or plain vectors; plain vectors are treated as if
wrapped with \code{\link[=vecvec]{vecvec()}}. All vectors in \code{.l} are recycled to a common
size following \code{\link[vctrs:vec_recycle_common]{vctrs::vec_recycle_common()}} rules.}

\item{.f}{A vectorised function, applied once per group of positions
that share the same underlying storage slots across every vector in
\code{.l}. Called with one vector per element of \code{.l} (that group's values)
plus \code{...}.}

\item{...}{Additional arguments passed to every call of \code{.f}.}

\item{ptype}{A prototype to simplify the result to with \code{\link[=unvecvec]{unvecvec()}}. If
\code{NULL} and \code{SIMPLIFY} is \code{TRUE}, the common type is inferred from the
result as in \code{\link[=unvecvec]{unvecvec()}}.}

\item{SIMPLIFY}{If \code{TRUE}, \code{\link[=unvecvec]{unvecvec()}} is applied to the result, using
\code{ptype} as the target type. If \code{FALSE}, the result is always a \code{vecvec}
object.}
}
\value{
A \code{vecvec} object combining the results of calling \code{.f} on each
group of positions of \code{.l}, or (if \code{SIMPLIFY} is \code{TRUE}) that result
simplified to a single type with \code{\link[=unvecvec]{unvecvec()}}. Positions where any
input has a missing (\code{NA}) index form their own group and are not
passed to \code{.f} at all - they come back as \code{NA} directly, matching how
missing elements are treated elsewhere (e.g. \code{vecvec_apply()}, \verb{[<-},
\verb{is.na<-}).
}
\description{
The \code{vecvec_mapply()} function applies a function \code{.f} across the
vectors in \code{.l}, combining the results into a vecvec that preserves the
original (possibly heterogeneous) type of each element of any \code{vecvec}
inputs. Unlike \code{mapply()}/\code{Map()}, \code{.f} is not called once per logical
element - it is called once per contiguous run of positions that draw
from the same combination of underlying storage slots across every
vector in \code{.l} (the same slot-grouping used internally for
\code{Ops.vecvec::vecvec}, e.g. \code{+}/\code{==}). Each call therefore receives one
vector per element of \code{.l} (that group's values) rather than one scalar
per element of \code{.l}, so \code{.f} must itself be vectorised - an ordinary
operator or vectorised function (e.g. \code{+}, \code{paste0}) qualifies
automatically, but \code{.f} must return a result the same length as its
inputs, or \code{vecvec_mapply()} errors.
}
\details{
Because the number of calls to \code{.f} scales with the number of distinct
slot combinations rather than with \code{length(.l[[1]])}, this is cheap for
structured or replicated inputs (few distinct slot combinations) and
degrades toward one call per element only in the worst case (every
position drawing from a different combination of slots) - the same cost
profile as \code{\link[=vecvec_apply]{vecvec_apply()}} and \code{Ops.vecvec::vecvec}, and unlike a plain
\code{mapply()}/\code{Map()} call, which always calls \code{.f} once per element.
}
\seealso{
\code{\link[=vecvec_apply]{vecvec_apply()}} for the single-input equivalent, applying \code{.f}
once per underlying storage vector of a single \code{vecvec}.
\code{Ops.vecvec::vecvec} uses the same slot-grouping strategy for
arithmetic/comparison operators.
}
